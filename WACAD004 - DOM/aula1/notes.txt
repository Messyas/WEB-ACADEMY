DOM - Especifica como vai ser o comportamento - O dom define
a estrutura logica de um documento e acessado e manipulado.

- Ele e um padrao w3c.
- E agnostico a linguagens de programacao.
- Armazenado em memoriar ram.

Tudo que se controi em reat vira um DOM apos o porte.

todo elemento, como text, h1 ou qualquer elemento, eles sao organizados em uma 
arvore.

exemplo: Element html -> Element body -> {element a -> attribute href || Text text}
